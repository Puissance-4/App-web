
{% extends 'base.html.twig' %}

{% block body %}
<br/>


<a href="{{path('accueil', {id : fiche.id})}}" type='button' class="btn btn-primary"><i class="fas fa-sign-out-alt"></i> Retour</a>
<br/><br/>


<!-- Affichage du formulaire permettant de modifier un frais forfaitisé -->

<h1>Editer le frais forfaitisé: {{fraisF.idType.libelle}}</h1><br/>
  
{{ form_start(form)}}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Valider</button>
{{ form_end(form) }}
 
{% endblock %}